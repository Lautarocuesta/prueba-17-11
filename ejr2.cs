//Ejercicio 2: Suma de matrices (List)
//Se solicita implementar un método en C# que reciba por parámetro dos matrices
//de números enteros y calcule la suma entre ambas estructuras devolviendo la
//matriz resultante. Posteriormente se solicita imprimir en la consola del
//sistema dicha matriz representando la misma de manera fidedigna en la consola
//del sistema. (i.e que se vea como matriz a nivel visual)


using System;
using System.Collections.Generic;

class Program {
  static int mat(num input){
List<int> numbers = new List<int>() { 1, 2, 5, 7, 8, 10 };
List<int> numbers1 = new List<int>() { 2, 2, 2, 1, 2, 10 };
  for(int i = 0; i < numbers.Count; i++){
      numbers1.add(num);
     }
  
  for(int i = 0; i < numbers1.Count; i++){
    numbers1.add();
    int sumatotal = 0;
}

    
  }

  };
  public static void Main (string[] args) {
    List<int>sumatotal = matrices();
   int sumatotal= mat(num);
    Console.WriteLine ("resultado:");
    
  }
